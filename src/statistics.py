# TODO compare results between different files and find distance

import datetime
import scipy.stats
import json
from pprint import pprint

# with open('maxPatchRecording_analysis.json') as data_file:
#     data = json.load(data_file)
#
# pprint(data['sfx']['inharmonicity'])
# dateTimeNow = datetime.datetime.now().strftime("%Y-%m-%d_%H:%M:%S")
